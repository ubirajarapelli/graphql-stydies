type Usuario {
  id: Int
  nome: String!
  email: String!
  idade: Int
  perfil: Perfil
  status: UsuarioStatus
}

type Perfil {
  id: Int
  nome: String
}

enum UsuarioStatus {
  ATIVO
  INATIVO
  BLOQUEADO
}
